{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "@now/next" }
  ],
  "routes": [
    { "src": "/post/(?<id>[^/]+)$", "dest": "/post?id=$id" },
    { "src": "/?page=(?<page>[^/]+)$", "dest": "/posts?page=$page" }
  ]
}